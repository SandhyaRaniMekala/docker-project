name: Docker Project Pipeline

on:
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{secrets.DOCKERHUB_sandhya9820}}
          password: ${{secrets.DOCKERHUB_dckr_pat_wYkQJ40--irkfsajXg0CvK0k8OY}}

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKERHUB_sandhya9820 }}/my-web-app:latest .

      - name: Puah Docker Image to Docker Hub
        run: docker push ${{ serets.DOCKERHUB_sandhya9820 }}/my-web-app:latest